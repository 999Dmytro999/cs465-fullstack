# Required in production. Use a unique random value with at least 32 characters.
# Development uses an ephemeral per-process secret when this is blank.
JWT_SECRET=

# Required in production. Separate multiple allowed origins with commas.
CLIENT_ORIGIN=http://localhost:4200

# Optional MongoDB host. Defaults to 127.0.0.1.
DB_HOST=127.0.0.1
